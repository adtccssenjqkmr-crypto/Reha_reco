workflows:
  android-release:
    name: Android Release Build
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      java: 21
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
    scripts:
      - name: Install npm dependencies
        script: |
          npm install
      - name: Sync Web Assets to Android Project
        script: |
          npx cap sync android
      - name: Build Android AAB
        script: |
          cd android
          chmod +x gradlew
          export JAVA_HOME=$(/usr/libexec/java_home -v 21)
          cp ../signing.keystore app/signing.keystore
          ./gradlew bundleRelease \
            -Pandroid.injected.signing.store.file="$(pwd)/app/signing.keystore" \
            -Pandroid.injected.signing.store.password="GSqYFW8RNgPT" \
            -Pandroid.injected.signing.key.alias="my-key-alias" \
            -Pandroid.injected.signing.key.password="GSqYFW8RNgPT"
    artifacts:
      - android/app/build/outputs/bundle/release/*.aab

  ios-release:
    name: iOS Release Build
    max_build_duration: 120
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: codemagic-admin
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.rehareco.myapp
      vars:
        XCODE_PROJECT: "ios/App/App.xcodeproj"
        XCODE_SCHEME: "App"
    scripts:
      - name: Install npm dependencies
        script: |
          npm install
      - name: Sync Web Assets to iOS Project
        script: |
          npx cap sync ios
      - name: Patch Development Team ID in Xcode project
        script: |
          sed -i '' 's/DEVELOPMENT_TEAM = "";/DEVELOPMENT_TEAM = 769Y9JYC3N;/g' ios/App/App.xcodeproj/project.pbxproj
      - name: Install CocoaPods (if Podfile exists)
        script: |
          if [ -f "ios/App/Podfile" ]; then
            cd ios/App && pod install
          else
            echo "No Podfile found, using Swift Package Manager (CapApp-SPM)"
          fi
      - name: Import Signing Certificate into Keychain
        script: |
          keychain initialize
          keychain add-certificates --certificate ios/dist_certificate.p12 --certificate-password codemagic
      - name: Install Provisioning Profile
        script: |
          PROFILES_HOME="$HOME/Library/MobileDevice/Provisioning Profiles"
          mkdir -p "$PROFILES_HOME"
          cp ios/profile.mobileprovision "$PROFILES_HOME/dc941172-1d69-4efd-a7a2-a1e850fb070f.mobileprovision"
          cp ios/profile.mobileprovision "$PROFILES_HOME/"
      - name: Build iOS App and Archive
        script: |
          sed -i '' 's/CODE_SIGN_STYLE = Automatic;/CODE_SIGN_STYLE = Manual;/g' ios/App/App.xcodeproj/project.pbxproj || true
          
          xcodebuild -project ios/App/App.xcodeproj \
            -scheme App \
            -config Release \
            -archivePath build/ios/xcarchive/App.xcarchive \
            archive \
            DEVELOPMENT_TEAM=769Y9JYC3N \
            CODE_SIGN_STYLE=Manual \
            PROVISIONING_PROFILE_SPECIFIER="RehaReco App Store V2" \
            PROVISIONING_PROFILE="dc941172-1d69-4efd-a7a2-a1e850fb070f"

          xcodebuild -exportArchive \
            -archivePath build/ios/xcarchive/App.xcarchive \
            -exportPath build/ios/ipa \
            -exportOptionsPlist ios/export_options.plist
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
